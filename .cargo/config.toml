[target.'cfg(target_os = "windows")']
rustflags = [
  "--cfg",
  "windows_slim_errors",
  "-C",
  "target-feature=+crt-static"
]

[build]
rustflags = ["-C", "target-cpu=native"]
[profile.release]
codegen-units = 1
debug = false
lto = true
opt-level = 's'
panic = "abort"
strip = true
